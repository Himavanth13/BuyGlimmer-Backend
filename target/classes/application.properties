spring.application.name=buyglimmer-backend

server.port=8080

# MariaDB dump target (overridable by environment variables)
spring.datasource.url=jdbc:mariadb://${DB_HOST:66.116.197.179}:${DB_PORT:3306}/${DB_NAME:sabbpeapparels}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.username=${DB_USER:sbuser}
spring.datasource.password=${DB_PASSWORD:}
spring.sql.init.mode=never
spring.h2.console.enabled=false

buyglimmer.brand=BuyGlimmer
buyglimmer.currency=INR
buyglimmer.support-email=support@buyglimmer.com
buyglimmer.token-prefix=bgm

springdoc.swagger-ui.path=/swagger-ui.html